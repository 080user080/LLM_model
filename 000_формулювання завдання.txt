## завдання

1. **Конфігурація (sfx.yaml)**  
   - Використовується **тільки секція `sounds`**.  
   - YAML містить перелік доступних тегів (`v`, `d`, `p2`, `r`, `v12` тощо) і відповідні їм файли.  
   - Цей файл є **спільним для кількох проєктів** і визначає, які теги взагалі можна використовувати.  
   - Інші параметри (`normalize_dbfs`, `gain_db`, `normalize`) у цьому контексті **ігноруються** — вони належать до іншого проєкту.

2. **Призначення тегів**  
   - Теги (`#v`, `#d`, `#p2`, `#r`, `#v12` …) вставляються у текст як маркери звукових ефектів.  
   - Вони відповідають ключам у словнику `sounds` з YAML.  
   - Якщо тегу немає у `sounds`, він вважається невідомим і має бути позначений/пропущений.

3. **GUI‑інтеграція**  
   - У `gui.py` додається кнопка **«Вставити теги ефектів»**.  
   - При натисканні:  
     - Завантажується `sfx.yaml`.  
     - З нього береться список доступних тегів.  
     - У текст вставляються відповідні теги (за правилами чи вручну користувачем).  
   - Результат: у полі «Оброблений текст» з’являються теги, які гарантовано існують у `sfx.yaml`.

4. **Валідація**  
   - Якщо користувач вставив тег, якого немає у `sounds`, система повідомляє про це у лог.  
   - Таким чином, `sfx.yaml` виступає як **каталог дозволених тегів**.

5. **Подальший розвиток**  
   - Можна додати підказки у GUI (наприклад, випадаючий список тегів з `sfx.yaml`).  
   - Можна зробити автоперевірку тексту: підсвічувати невідомі теги.  
   - Параметри гучності/нормалізації залишаються поза цим проєктом.


👉 Отже, у цьому проєкті **головне завдання**:  
**читати з `sfx.yaml` список тегів у секції `sounds` і дозволяти вставляти тільки ті теги у текст через GUI.**  


---

# Мета

Зчитувати з **sfx.yaml** (спільного для кількох проєктів) секцію `sounds` і дозволяти через GUI вставляти в текст **тільки** ті теги, що присутні у `sounds`. Інші поля YAML не використовуються для обробки звуків у цьому проєкті, але можуть бути присутніми і відображатися в UI як інформація.

---

# Формат і правила (для цього проєкту)

* Файл `sfx.yaml` може містити глобальні параметри (наприклад `normalize_dbfs: -16`) — **ігноруються** в логіці вставки/валідації тут.
* Єдина секція, яку використовуємо — **`sounds`**. Вона може виглядати як у прикладі користувача:

  ```
  normalize_dbfs: -16

  sounds:
    v:
      file: "бах.mp3"
      gain_db: 0

    d:
      file: "рип.mp3"
      gain_db: 0
      normalize: false

    p2:
      file: "пауза2сек.mp3"
      gain_db: 0

    r:
      file: "Розділ.mp3"
      gain_db: 0
  ```
* Для цього проєкту ключі в `sounds` (наприклад `v`, `d`, `p2`, `r`) — **єдиний перелік дозволених тегів**. Поля всередині кожного ключа (`file`, `gain_db`, `normalize` тощо) можуть існувати, але обробка звуку/налаштування гучності — **поза задачею**.
* Теги в тексті пишуться як `#v`, `#d`, `#p2` тощо. У `sfx.yaml` — без `#`.

---

# Поведінка GUI

1. Кнопка **«Вставити теги ефектів»**:

   * Завантажує `sfx.yaml`.
   * Читає ключі з `sounds` і показує їх як список (чекбокси / випадайка / typeahead).
   * При вставці додає в текст тільки `#<ключ>` для ключів, що є в `sounds`.


2. Режими вставки:

   * **Ручна вставка** — користувач вибирає теги і вставляє в позицію курсору.
   * **Пакетна вставка / пропозиції** — (опціонально) UI може пропонувати теги за ключовими словами, але вставка відбувається тільки після підтвердження користувача.

3. Підказки:
   * Фільтр-пошук по тегах.


# Валідація і логіка обробки

* Джерело істини — **ключі у `sounds`**.
* При аналізі тексту:

  * Визначати всі `#tag` (синтаксис: `#` + буквено-цифровий/underscore).
  * Якщо тег є в `sounds` — вважати **валідним**.
  * Якщо тегу немає в `sounds` — вважати **невідомим** → створювати запис у лог (`WARN`) та підсвічувати/помічати в UI.
* Поведінка при невідомих тегах:

  * Не блокувати збереження тексту.
  * При експорті/рендері — показати попередження: «є невідомі теги».
  * Лог повинен містити: таймстемп, назву файлу/розділу, невідомі теги (і, якщо бажано, позиції/рядки).



# UX: підсвітка і звіти

* В реальному часі підсвічувати невідомі теги (червоним/underline) і показувати у tooltip повідомлення «Тег не знайдено в sfx.yaml».
* Додати в меню «Перевірити теги» — звіт: кількість валідних / невідомих тегів + експорт списку невідомих у CSV/лог.

---

# Лог-файл (рекомендація)

* Легкий текстовий лог у проєктній папці (формат plain text). Записи мінімально необхідні:

  * `INFO Loaded sfx.yaml (n tags=...)`
  * `WARN Unknown tag '#xyz' in file chapter_01.txt line X`

---

# Обмеження (важливо)

* Жодних змін гучності, нормалізації чи трансформацій звуку — **цей проєкт тільки про валідацію і вставку тегів**. Поля `gain_db`, `normalize` лишаються метаданими — show-only.
* `sfx.yaml` — **спільний** для кількох проєктів: не видаляти/не змінювати його структуру без узгодження з іншими проєктами.

---

# Кроки впровадження (технічна дорожня карта, коротко)

1. UI: кнопка «Вставити теги ефектів» з модалкою списку тегів з `sounds`.
2. Завантаження `sfx.yaml` (тільки `sounds`).
3. Правила вставки: тільки `#<ключи зі sounds>`.
4. Розділ валідації: підсвітка невідомих тегів + логування WARN.

---